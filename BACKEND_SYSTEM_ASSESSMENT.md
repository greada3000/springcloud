# 社区后端系统评估与整改建议

> 评估日期：2026-08-19  
> 评估对象：`community-api`（Spring Boot 单体应用）  
> 评估方式：仓库静态审查、配置与数据库脚本检查、现有测试检查；未进行性能压测、安全渗透、生产环境与真实数据检查。

## 1. 执行摘要

该系统已完成从 Spring Cloud 多服务到单体 Spring Boot 的合并，代码量小、分层直接，适合作为教学、原型或低风险内部系统的基础。统一响应、Bean Validation、BCrypt、事务、分页插件、OpenAPI 和数据库索引等基础能力已经具备。

但系统目前**不具备生产上线条件**。最主要的问题不是代码风格，而是访问控制边界缺失：登录成功后不签发凭证，后续接口也不验证身份和资源所有权，调用者可以直接伪造用户 ID，修改或删除任意业务数据。数据库默认管理账号和弱密码也会放大风险。此外，数据外键约束、接口输入边界、错误状态语义、可观测性、自动化测试、交付与容灾能力均不完整。

综合静态评分：**42/100（高风险，建议完成 P0/P1 整改后再上线）**。

### 1.1 维度评分

| 维度 | 权重 | 得分 | 加权得分 | 结论 |
|---|---:|---:|---:|---|
| 业务与功能 | 20% | 60 | 12.0 | 五个核心模块齐全，但权限、级联规则和部分业务约束缺失 |
| 架构设计 | 15% | 65 | 9.8 | 小规模系统采用单体合理，分层清晰；DTO/领域边界偏弱 |
| 性能与容量 | 15% | 40 | 6.0 | 有分页插件和部分索引，但多个列表无分页，模糊搜索难扩展，无基准数据 |
| 可用性与可靠性 | 15% | 30 | 4.5 | 有局部事务；无健康检查、超时治理、备份恢复和容量验证 |
| 安全与合规 | 15% | 15 | 2.3 | BCrypt 是优点，但认证、授权、防爆破、生产凭据管理均缺失 |
| 可维护性 | 10% | 45 | 4.5 | 结构简单、文档初具备；请求/响应复用实体，测试覆盖严重不足 |
| 可观测与运维 | 5% | 15 | 0.8 | 未见 Actuator、指标、审计、链路标识、告警和交付配置 |
| 成本与团队适配 | 5% | 50 | 2.5 | 单体运维成本低，但人工发布和故障定位会形成隐性成本 |
| **合计** | **100%** |  | **42.4** | **按 42/100 计** |

> 评分反映当前仓库的“生产就绪度”，不是业务价值评分。没有运行数据的项目按“能力未证明”计分，而不是断言其性能一定不合格。

## 2. 评估范围与证据基线

### 2.1 系统概况

- Java 17、Spring Boot 3.5.16、MyBatis-Plus 3.5.17、MySQL 8。
- 单体分层结构：Controller → Service → Mapper → MySQL。
- 业务模块：用户、圈子、文章、评论、关注关系。
- 规模：主代码 33 个 Java 文件、约 794 行；测试代码 1 个文件、约 32 行。
- 数据库：5 张业务表，由 `database/init.sql` 初始化。

### 2.2 验证限制

- 当前环境找不到 `mvn` 可执行命令，且仓库没有 Maven Wrapper，因此本次未能重新执行 `mvn test`。
- `target/surefire-reports` 中没有可复核的测试报告。`API_DOCUMENTATION.md` 中记录过 1 个测试成功，但只能视为历史陈述。
- 未提供生产配置、部署拓扑、真实流量、慢 SQL、监控、日志、事故、备份恢复或 SLA 数据。
- 未连接数据库执行约束验证，也未进行 DAST、SAST、依赖漏洞扫描和压力测试。

## 3. 主要优点

1. **架构简化符合当前规模。** 从多服务合并成单体，去掉注册中心和网关，减少了网络调用、部署单元和故障点。对于不足千行主代码的系统，这一方向合理。
2. **基础分层清楚。** Controller 依赖 Service 接口，事务主要位于 Service 层，模块职责容易理解。
3. **密码已使用 BCrypt。** `ApplicationConfig` 提供 `BCryptPasswordEncoder`，注册和修改密码都会散列保存；`User.password` 设置为只写，正常 JSON 响应不会暴露密码。
4. **写操作部分具备事务边界。** 文章删除与评论删除处于同一事务，能避免应用层级联执行一半。
5. **已有基础数据库索引。** 主键、文章用户/圈子、评论文章/用户以及关注唯一关系均有索引。
6. **接口可发现性较好。** 有统一 `/api` 前缀、OpenAPI UI、统一响应模型和接口清单。
7. **查询采用 MyBatis-Plus 条件构造器。** 当前代码没有发现直接拼接用户输入的 SQL，经典 SQL 注入风险相对较低。

## 4. 分维度评估

### 4.1 业务适配性与功能正确性

**现状**

- 五个基础业务域均有增删改查或搜索接口。
- 关注关系通过 `(preuser, lastuser)` 唯一索引防止数据库层重复。
- 文章删除会在同一事务内先删除评论。

**问题与风险**

- 缺少明确的角色和资源所有权规则。客户端可提交 `owner`、`userId`、`username`、`ownerId`、`preuser` 等身份字段，服务端直接信任。
- 更新接口允许修改资源归属：文章可修改 `userId`，评论可修改 `ownerId` 和 `articleId`，圈子可修改 `owner`。
- 删除用户或圈子没有定义关联文章、评论、关注关系的处理策略；当前数据库没有外键，可能直接留下孤儿数据。
- 创建文章同时存储 `userId` 和 `username`，用户名更新后文章冗余字段不会同步，可能显示旧名称。
- 创建评论、文章、圈子和关注关系之前不验证引用用户、文章或圈子是否存在。
- `GET /api/reviews/article/{id}`、用户文章、圈子文章、粉丝与关注列表均返回完整列表，数据增长后会失控。
- 注册由客户端指定 `userId`；是否符合业务上的账号生成与防枚举要求尚未定义。

**建议**

- 以认证主体为准设置作者/所有者字段，禁止客户端决定归属；管理员操作另设权限。
- 明确用户、圈子、文章删除策略：禁止删除、软删除、级联删除或匿名化四选一，并用数据库约束与事务共同保证。
- 将用户名从文章写模型中移除，查询时关联用户或采用明确的快照字段语义。
- 所有集合接口统一分页，并定义默认值、最大页大小、排序字段与游标方案。

### 4.2 系统架构

**评价**

当前体量采用单体优于维持 Spring Cloud 拆分，部署和调试成本较低。问题主要在应用内部边界，而非是否需要重新微服务化。

**问题与建议**

- 数据库实体同时充当创建请求、更新请求和响应模型，造成过度赋值和敏感字段风险。建议拆分 `CreateXxxRequest`、`UpdateXxxRequest`、`XxxResponse`。
- 领域规则分散在简单 CRUD 服务中。建议将所有权、删除策略、状态转换放在 Service 层统一执行。
- XML Mapper 当前只声明未被显式引用的 `resultMap`，增加维护噪声。若完全依赖 MyBatis-Plus，可删除无用 XML；若保留复杂查询，应明确绑定和测试。
- 暂不建议重新拆成微服务。优先建设安全、测试和运维基线；只有当团队、流量、发布节奏或数据边界明确要求独立扩缩容时再拆分。

### 4.3 性能与容量

**已确认风险**

- 多个列表接口使用 `selectList`，没有分页上限。
- `page` 和 `size` 未校验，可能传入 0、负数或超大值。
- 文章搜索对标题、正文、用户名执行前后模糊 `LIKE`；普通 B-Tree 索引无法有效支持 `%keyword%`，数据量增加后容易全表扫描。
- 文章使用随机 UUID 字符串作为聚簇主键；在 InnoDB 中会增加索引体积并产生随机写入。
- 文章按随机 UUID 倒序并不等于按发布时间倒序，且实体未映射 SQL 中已有的 `created_at`、`updated_at`。

**建议指标与验证**

- 对全部分页入口约束 `page >= 1`、`1 <= size <= 100`。
- 列表只返回摘要字段，正文仅在详情接口读取。
- 以 `created_at DESC, article_id DESC` 稳定排序，建立相应联合索引；大数据量时采用游标分页。
- 搜索量较小时先用 MySQL FULLTEXT；确有中文分词、相关度和高写入量要求时再引入专业搜索引擎。
- 用真实比例数据执行 `EXPLAIN ANALYZE` 和压测。首轮建议目标：核心读接口 P95 < 300 ms、写接口 P95 < 500 ms、错误率 < 0.1%，并验证峰值 2 倍流量持续 30 分钟。最终阈值须由业务 SLA 确认。

### 4.4 可用性与可靠性

**问题**

- 没有 Actuator 健康检查、优雅停机、就绪/存活探针或连接池监控配置。
- 未见数据库迁移工具，`init.sql` 同时承担建表和测试数据灌入；无法可靠追踪生产数据库版本。
- 未提供数据库备份、恢复演练、RTO/RPO 或发布回滚方案。
- 除文章删除外，跨实体一致性规则主要依赖应用约定；没有外键保护。
- 更新无版本号或条件更新，并发修改存在最后写入覆盖风险。

**建议**

- 引入 Flyway/Liquibase，生产环境只执行版本化迁移；测试数据使用独立 profile 或脚本。
- 添加 Actuator，仅对受控网络暴露 `health`、`info`、`metrics`；配置优雅停机与探针。
- 对核心更新增加乐观锁版本字段；为唯一业务约束建立数据库唯一键，并正确处理冲突。
- 明确 RPO/RTO，建立自动备份、异地保存和季度恢复演练。恢复成功应以实际还原并校验数据为准。

### 4.5 安全性

**P0 问题**

- 系统没有 Spring Security Web 安全链、JWT/Session、鉴权注解或权限校验。`/login` 只校验一次密码并返回用户对象，不产生可用于后续认证的凭证。
- 所有修改和删除接口均匿名可调用；可修改任意用户类型，形成直接提权路径。
- 数据库连接默认 `root/123456`，README 和接口文档公开了该默认值；若部署时漏配环境变量，应用会以数据库超级用户弱密码运行。

**其他风险**

- 登录失败均使用 `IllegalArgumentException`，但不存在登录频率限制、失败锁定、审计和验证码策略。
- 修改密码仅检查旧密码，没有密码强度、凭证撤销和修改后重新登录机制。
- CORS 来源虽在配置文件声明，但代码中没有读取或注册 CORS 配置；该配置目前是“看似可配、实际未生效”。
- Swagger/OpenAPI 默认公开，生产环境会扩大接口枚举面。
- 测试账号和固定弱密码由初始化脚本写入；生产误执行后会产生已知管理员账号。
- 缺少审计日志、安全响应头、请求体大小限制、依赖漏洞扫描和敏感配置启动校验。

**建议方案**

- 引入 `spring-boot-starter-security`，采用短期访问令牌 + 可撤销刷新令牌，或服务端 Session；密码登录成功后只返回令牌和脱敏用户信息。
- 建立 RBAC 和资源所有权校验：普通用户只能修改自己的资料及自己创建的资源，管理员权限必须显式授予。
- 服务端从认证上下文取得 user ID，忽略或拒绝请求体中的身份归属字段。
- 生产环境取消所有数据库凭据默认值，使用专用最小权限账号和 Secret 管理；缺少必需 Secret 时启动失败。
- 对登录和敏感操作实施限流、失败审计、统一模糊错误消息和令牌撤销；密码长度建议至少 12 位并支持泄露密码检查。
- CORS 使用类型安全配置绑定并注册到安全链；生产仅允许明确 HTTPS 来源。
- 通过 profile 关闭生产 Swagger，测试数据只允许在 `dev/test` 环境写入。

### 4.6 数据与存储设计

**问题**

- 五张表之间没有外键，无法阻止不存在的用户、圈子或文章被引用。
- 没有通用审计字段；只有文章具有创建和更新时间，但 Java 实体没有映射它们。
- 关注关系没有 `created_at`，难以支持时间排序和审计。
- 多个字符串字段只有数据库长度限制，Java 校验只有 `@NotBlank`，超长输入最终由数据库异常处理。
- `init.sql` 的 `ON DUPLICATE KEY UPDATE` 会反复重置测试账号密码，不适合作为生产迁移脚本。

**建议**

- 增加外键或等价的数据库约束，并为删除行为显式指定 `RESTRICT`、`CASCADE` 或软删除策略。
- 补全 `created_at`、`updated_at`、必要时 `deleted_at/version`，在 API 中统一 ISO-8601 时间格式。
- 使用 `@Size` 等校验与数据库长度保持一致；数据库异常转换为稳定的业务错误码。
- 将 schema migration、开发种子数据和测试夹具拆开。

### 4.7 API 与集成能力

**问题**

- 不存在资源时抛出 `IllegalArgumentException` 并返回 HTTP 400，正确语义通常应为 404；未认证为 401、无权限为 403、唯一冲突为 409。
- 成功创建仍返回 200，建议返回 201 和资源地址；删除可返回 204。
- 错误响应没有稳定 `code`、字段级校验详情和 `traceId`，前端只能匹配中文消息。
- 更新接口未使用 `@Valid`，且完整实体作为部分更新请求，PUT/PATCH 语义模糊。
- 兼容旧版的 POST 搜索接口没有废弃计划或版本边界。

**建议**

- 定义统一错误模型：`code`、`message`、`fieldErrors`、`traceId`、`timestamp`，并映射正确状态码。
- 创建/更新专用 DTO；PUT 表示整体替换，PATCH 表示局部更新。
- 对 API 建立 `/api/v1` 版本或明确的兼容策略，并为旧接口标注 deprecated 和下线日期。
- 为 OpenAPI 补充认证方案、状态码、错误模型和示例，并增加契约测试。

### 4.8 可维护性与测试

**现状与问题**

- 代码结构简单，Lombok 减少模板代码，文档列出了接口和迁移背景。
- 只有一个依赖真实本地 MySQL 与固定种子数据的 `@SpringBootTest`，主要验证读取成功；没有隔离、回滚说明，也没有覆盖创建、更新、删除、事务回滚、校验、异常、权限和并发。
- README 直接依赖本机固定 MySQL 安装路径和密码，团队或 CI 环境难以复现。
- 本次无法运行测试，说明仓库缺少自带的 `mvnw` 也降低了构建可复现性。

**建议测试金字塔**

- Service 单元测试：权限、所有权、边界和异常分支。
- Web 层测试：MockMvc 验证参数、状态码、认证和响应契约。
- Repository 集成测试：Testcontainers MySQL 验证真实 SQL、索引和迁移。
- 关键端到端测试：注册/登录/发文/评论/关注/删除全链路。
- 并发测试：重复关注、重复注册、并发更新和级联删除。
- CI 门禁：编译、测试、格式检查、静态分析、依赖漏洞和数据库迁移验证。

### 4.9 可观测性、部署与运维

**问题**

- 未见结构化日志、请求 ID/trace ID、业务指标、审计日志、告警规则和看板。
- 未见 Dockerfile、Compose、CI/CD、环境 profile 或部署清单。
- 未配置日志脱敏和保留策略。

**建议**

- 为每个请求生成/透传 `traceId`，结构化记录方法、路径、状态码、耗时、主体和错误码，禁止记录密码与令牌。
- 采集 JVM、连接池、HTTP 延迟/错误率、慢 SQL及登录失败等指标；为可用性、错误率和资源饱和度设置告警。
- 增加可重复构建方式（Maven Wrapper 或固定构建镜像）、多阶段容器镜像、非 root 用户和只读文件系统。
- 建立 dev/test/prod 配置分层、CI/CD、数据库迁移、灰度或滚动发布与回滚手册。

### 4.10 成本与团队能力

单体 + MySQL 的直接成本较低，现阶段不需要增加注册中心、消息队列或分布式缓存。优先投入认证授权、数据约束、测试和监控，可获得最高风险收益比。只有在压测证明数据库搜索或热点读取成为瓶颈后，再考虑全文搜索、缓存或读写分离，避免为假设性规模支付长期复杂度成本。

## 5. 整改优先级

优先级定义：P0 为上线阻断；P1 为上线前必须完成；P2 为上线后近期治理；P3 为规模增长或体验优化。

### 5.1 整改清单

| ID | 优先级 | 问题 | 主要措施 | 验收标准 | 建议工作量 |
|---|---|---|---|---|---:|
| SEC-01 | P0 | 无认证与授权 | 接入 Security；签发/校验凭证；RBAC + 资源所有权 | 匿名写请求 401；越权写请求 403；普通用户不能修改他人资源；管理员权限有自动化测试 | 5–8 人日 |
| SEC-02 | P0 | 默认数据库 root/弱密码 | prod 无默认值；最小权限账户；Secret 管理；启动校验 | 缺少生产 Secret 启动失败；应用账号无建库和用户管理权限；仓库无生产密钥 | 1–2 人日 |
| SEC-03 | P0 | 已知测试管理员可能进入生产 | 拆分 schema 与 seed；seed 仅 dev/test 可执行 | 生产迁移不创建测试账号；发布检查能阻断 seed 脚本 | 1 人日 |
| DATA-01 | P1 | 引用完整性和删除规则缺失 | 外键/软删除；定义用户、圈子、文章级联策略 | 无法写入孤儿引用；删除用例覆盖全部关联数据；迁移可回滚 | 3–5 人日 |
| API-01 | P1 | 服务端信任身份字段、实体直接作 DTO | 创建/更新 DTO；主体 ID 来自认证上下文；禁止修改归属 | 请求伪造 owner/userId 无效或 400；响应不包含内部敏感字段 | 3–5 人日 |
| API-02 | P1 | 状态码与错误模型不准确 | 业务异常体系；401/403/404/409；稳定错误码与 traceId | OpenAPI 与测试覆盖主要成功/失败状态；前端不依赖中文消息判断 | 2–3 人日 |
| PERF-01 | P1 | 无界列表与分页参数无限制 | 全列表分页；size 上限；稳定排序 | 所有集合接口分页；`size > 100` 被拒绝或截断；大数据量内存稳定 | 2–3 人日 |
| TEST-01 | P1 | 自动化测试不足且依赖本机库 | Testcontainers、MockMvc、单元与集成测试、CI | 干净机器一条命令通过；核心权限/事务/错误路径均覆盖；CI 必须通过 | 5–8 人日 |
| OPS-01 | P1 | 无健康检查和生产观测基线 | Actuator、结构化日志、traceId、核心指标和告警 | 探针可用且受限；能从一次失败请求定位日志；关键告警演练成功 | 3–5 人日 |
| DATA-02 | P1 | 无版本化数据库迁移 | Flyway/Liquibase；拆分迁移与种子数据 | 空库可自动升级；重复执行安全；CI 验证迁移 | 2–3 人日 |
| SEC-04 | P1 | 登录防护、CORS 和 Swagger 暴露策略缺失 | 登录限流/审计；实际注册 CORS；prod 关闭或保护文档 | 爆破触发限流；非白名单来源失败；prod 匿名不可访问 API 文档 | 2–4 人日 |
| REL-01 | P2 | 并发更新覆盖 | 乐观锁和冲突响应 | 两个并发更新有一个返回 409，不静默覆盖 | 2 人日 |
| PERF-02 | P2 | 文章查询与排序不可扩展 | 时间字段映射、联合索引、查询计划、搜索方案 | 目标数据量下查询计划可解释，P95 达到约定目标 | 3–5 人日 |
| OPS-02 | P2 | 无可重复交付与回滚 | Maven Wrapper/构建镜像、容器化、CI/CD、回滚手册 | 干净环境可构建；镜像非 root；一次发布和回滚演练成功 | 3–5 人日 |
| REL-02 | P2 | 备份恢复目标缺失 | 定义 RPO/RTO、自动备份、恢复演练 | 恢复演练在 RTO 内完成，数据损失不超过 RPO | 2–3 人日 + 运维 |
| API-03 | P2 | 版本与兼容接口缺少退出机制 | API v1 或兼容政策；标记并下线旧 POST 搜索 | 文档注明废弃日期；调用监控归零后删除旧接口 | 1–2 人日 |
| PERF-03 | P3 | UUID 字符串主键和搜索成本 | 基于实测评估有序 ID/BINARY UUID、全文检索 | 仅在压测证明收益后实施，迁移方案无停机或可控停机 | 待评估 |

> 工作量为单名熟悉 Spring Boot 的开发人员粗略估算，不含需求确认、跨团队联调和审批时间。

### 5.2 推荐执行顺序

1. **第 1 阶段：封住安全边界（P0）**  
   认证授权、所有权规则、生产 Secret、移除生产测试账号。在此之前不建议向公网或真实用户开放。
2. **第 2 阶段：保证数据与接口正确（P1）**  
   DTO、错误语义、外键/删除策略、分页、数据库迁移。
3. **第 3 阶段：建立可验证交付（P1）**  
   自动化测试、CI、健康检查、日志指标与告警。随后进行一次发布、回滚和故障演练。
4. **第 4 阶段：用数据优化（P2/P3）**  
   生成接近生产规模的数据，执行 SQL 分析与压测，再决定全文搜索、缓存、主键改造等方案。

## 6. 建议的上线门禁

以下条件全部满足后，才建议进入生产发布评审：

- P0 全部关闭，且有自动化负向测试证明匿名和越权访问被阻止。
- P1 中安全、数据一致性、分页、迁移、测试和可观测项目全部完成。
- 构建、测试、数据库迁移和依赖漏洞扫描在 CI 中自动执行。
- 使用非 root 数据库账号，生产 Secret 不进入仓库或镜像。
- 完成至少一次备份恢复、发布回滚和数据库迁移演练。
- 在约定数据量和峰值并发下完成压测，P95/P99、吞吐、错误率和资源利用率达到团队确定的 SLO。
- 明确值班负责人、告警接收渠道和关键故障运行手册。

## 7. 90 天整改路线图

| 周期 | 目标 | 交付物 |
|---|---|---|
| 第 1–2 周 | 关闭 P0 安全风险 | 认证授权、所有权策略、Secret 管理、生产禁用测试种子 |
| 第 3–4 周 | 修正数据与 API 契约 | DTO、异常码、外键/软删除策略、全量分页、迁移基线 |
| 第 5–6 周 | 补齐质量门禁 | 单元/Web/集成测试、Testcontainers、CI、Maven Wrapper 或固定构建镜像 |
| 第 7–8 周 | 建立运行保障 | Actuator、结构化日志、traceId、指标、告警、审计日志 |
| 第 9–10 周 | 性能与容量验证 | 测试数据模型、SQL 分析、负载/峰值/稳定性测试报告 |
| 第 11–12 周 | 上线演练与验收 | 备份恢复、发布回滚、故障演练、SLO 与运行手册、风险复审 |

## 8. 结论

该项目的单体化方向和基础代码组织是合理的，不需要优先做复杂架构重构。当前整改核心应是把“能运行的 CRUD”提升为“身份可信、数据可靠、可验证、可观测、可恢复的服务”。建议先用 2 周解决 P0，再完成 P1 基线并进行压测与演练；在运行数据证明瓶颈之前，不建议引入微服务、Redis、消息队列或 Elasticsearch。

